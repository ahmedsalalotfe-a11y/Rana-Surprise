<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mohamed & Rana Forever ❤️</title>
    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.5.1/dist/confetti.browser.min.js"></script>
    <style>
        :root {
            --primary: #ff2a55;
            --accent: #ff758f;
        }

        body {
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #ffccd5 0%, #ff4d6d 100%);
            color: #4a001f;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow-x: hidden;
        }

        #pass-screen {
            background: rgba(255, 255, 255, 0.95);
            padding: 40px 30px;
            border-radius: 30px;
            text-align: center;
            box-shadow: 0 20px 40px rgba(0,0,0,0.25);
            border: 4px solid var(--accent);
            width: 85%;
            max-width: 360px;
            z-index: 100;
        }

        input[type="password"] {
            width: 80%;
            padding: 14px;
            margin: 20px 0;
            border: 2px solid var(--primary);
            border-radius: 20px;
            text-align: center;
            font-size: 20px;
            outline: none;
        }

        .btn-main {
            background: linear-gradient(45deg, #ff2a55, #ff758f);
            color: white;
            border: none;
            padding: 14px 35px;
            border-radius: 25px;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            box-shadow: 0 8px 20px rgba(255, 42, 85, 0.4);
            transition: 0.3s;
        }

        .btn-main:hover { transform: scale(1.05); }

        #main-content {
            display: none;
            width: 90%;
            max-width: 550px;
            margin: 20px 0;
            z-index: 10;
        }

        .card {
            background: rgba(255, 255, 255, 0.94);
            padding: 25px;
            border-radius: 25px;
            margin-bottom: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.15);
            text-align: center;
            border: 2px solid #ffccd5;
        }

        /* تصميم الجاتوه/التورتة */
        .cake-container {
            position: relative;
            width: 200px;
            height: 180px;
            margin: 20px auto;
            cursor: pointer;
            user-select: none;
        }

        .cake-layer {
            position: absolute;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 12px;
            transition: all 0.5s ease;
        }

        .layer-bottom {
            bottom: 0;
            width: 180px;
            height: 50px;
            background: #ff758f;
            border-bottom: 6px solid #d60036;
        }

        .layer-middle {
            bottom: 45px;
            width: 130px;
            height: 45px;
            background: #ffb3c1;
            border-bottom: 5px solid #ff4d6d;
        }

        .layer-top {
            bottom: 85px;
            width: 80px;
            height: 40px;
            background: #fff;
            border-bottom: 4px solid #ff758f;
        }

        .candle {
            position: absolute;
            bottom: 125px;
            left: 50%;
            transform: translateX(-50%);
            width: 12px;
            height: 30px;
            background: linear-gradient(to bottom, #ffea00, #ff9100);
            border-radius: 4px;
        }

        .flame {
            position: absolute;
            bottom: 155px;
            left: 50%;
            transform: translateX(-50%);
            width: 14px;
            height: 20px;
            background: #ff4d00;
            border-radius: 50% 50% 20% 20%;
            box-shadow: 0 0 10px #ffea00, 0 0 20px #ffea00;
            animation: flicker 0.6s infinite alternate;
        }

        @keyframes flicker {
            0% { transform: translateX(-50%) scale(1); }
            100% { transform: translateX(-50%) scale(1.2); }
        }

        .cake-container.cut .layer-bottom { transform: translateX(-80%) rotate(-5deg); }
        .cake-container.cut .layer-middle { transform: translateX(-75%) rotate(-5deg); }
        .cake-container.cut .layer-top { transform: translateX(-70%) rotate(-5deg); }
        .cake-container.cut .flame { opacity: 0; }

        .knife {
            position: absolute;
            top: -20px;
            right: -20px;
            font-size: 40px;
            opacity: 0;
            transition: all 0.4s ease;
            pointer-events: none;
        }

        .cake-container:hover .knife { opacity: 1; transform: translate(-20px, 20px) rotate(-45deg); }

        /* لعبة القلوب */
        #game-board {
            width: 100%;
            height: 220px;
            background: #ffe6eb;
            border-radius: 20px;
            position: relative;
            overflow: hidden;
            border: 2px dashed var(--primary);
            margin-top: 15px;
            cursor: pointer;
        }

        .heart-target {
            position: absolute;
            font-size: 38px;
            user-select: none;
        }

        /* البلالين */
        .balloon {
            position: fixed;
            bottom: -100px;
            font-size: 40px;
            user-select: none;
            pointer-events: none;
            z-index: 50;
            animation: floatUp 6s linear infinite;
        }

        @keyframes floatUp {
            0% { transform: translateY(0) rotate(0deg); opacity: 1; }
            100% { transform: translateY(-120vh) rotate(360deg); opacity: 0; }
        }

        .timer-grid {
            display: flex;
            justify-content: space-around;
            margin-top: 10px;
            flex-wrap: wrap;
            gap: 10px;
        }

        .timer-card {
            background: #fff0f3;
            padding: 10px 12px;
            border-radius: 15px;
            border: 1px solid var(--accent);
            font-weight: bold;
            flex: 1;
            min-width: 60px;
        }

        .timer-card span {
            display: block;
            font-size: 20px;
            color: var(--primary);
        }
    </style>
</head>
<body>

    <div id="pass-screen">
        <div style="font-size: 50px;">👑✨</div>
        <h2 style="color: var(--primary);">مفاجأة خاصة لرنا ❤️</h2>
        <p>اكتبي الباسورد عشان تفتحي الهدية</p>
        <input type="password" id="pass-input" placeholder="262026">
        <br>
        <button class="btn-main" onclick="openSurprise()">افتحي المفاجأة 🎉</button>
    </div>

    <div id="main-content">

        <div class="card">
            <h1 style="color: var(--primary); margin:0;">👑 كل سنة وأنتِ طيبة يا رنا 👑</h1>
            <p style="font-size:1.2em; font-weight:bold; color:#d60036;">💘 Mohamed & Rana Forever 💘</p>
        </div>

        <!-- عداد عمر رنا -->
        <div class="card">
            <h3>🎈 عمر رنا اليوم: ✨</h3>
            <p style="font-size: 0.9em; color: #666; margin-bottom: 10px;">منذ يوم ميلادك الجميل (18/8/2010):</p>
            <div class="timer-grid">
                <div class="timer-card"><span id="age-years">0</span>سنة</div>
                <div class="timer-card"><span id="age-days">0</span>يوم</div>
                <div class="timer-card"><span id="age-hours">0</span>ساعة</div>
                <div class="timer-card"><span id="age-minutes">0</span>دقيقة</div>
            </div>
        </div>

        <!-- تقطيع الجاتوه -->
        <div class="card">
            <h3>🎂 قطّعي تورتة عيد الميلاد يا رنا! 🔪</h3>
            <p style="font-size: 0.95em; color: #666;">اضغطي على التورتة عشان تقطعيها وتولّعي الألعاب النارية ✨</p>
            
            <div class="cake-container" id="cake" onclick="cutCake()">
                <div class="knife">🔪</div>
                <div class="flame" id="flame"></div>
                <div class="candle"></div>
                <div class="cake-layer layer-top">🎂</div>
                <div class="cake-layer layer-middle"></div>
                <div class="cake-layer layer-bottom"></div>
            </div>

            <p id="cake-status" style="font-weight: bold; color: var(--primary); margin-top: 10px;">اضغطي هنا لتقطيع الجاتوه! 💖</p>
        </div>

        <div class="card">
            <h3>🎵 موسيقى الحب 🎶</h3>
            <audio controls id="bg-audio" loop style="width:100%;">
                <source src="https://cdn.pixabay.com/download/audio/2022/05/27/audio_1808fbf07a.mp3?filename=romantic-piano-112188.mp3" type="audio/mpeg">
            </audio>
        </div>

        <div class="card">
            <h3>🎮 لعبة: اصطادي قلوب Mohamed 💖</h3>
            <div style="display:flex; justify-content:space-around; font-weight:bold; font-size:1.1em; color:var(--primary);">
                <div>النقاط: <span id="score">0</span> 💘</div>
                <div>المستوى: <span id="level">1</span> ⭐</div>
            </div>
            <div id="game-board" onclick="missClick()">
                <div id="heart" class="heart-target" onclick="catchHeart(event)">💖</div>
            </div>
            <p id="game-status" style="font-weight:bold; color:var(--primary); margin-top:10px;">اضغطي على القلوب السريعة قبل ما تختفي!</p>
        </div>

        <div class="card">
            <h3>💌 رسالة خاصة جداً 💌</h3>
            <p style="line-height: 1.8; color: #333; font-size:1.05em;">
                كل سنة وأنتِ طيبة وبخير يا أجمل رنا في الدنيا. ربنا يديمك في حياتي ويفضل Mohamed ورنا مع بعض للأبد إن شاء الله. 💘
            </p>
        </div>

    </div>

    <script>
        const PASS = "262026";
        const BIRTH_DATE = new Date("2010-08-18T00:00:00");
        let score = 0;
        let level = 1;
        let gameInterval;
        let isCakeCut = false;

        function openSurprise() {
            const input = document.getElementById("pass-input").value;
            if (input === PASS) {
                document.getElementById("pass-screen").style.display = "none";
                document.getElementById("main-content").style.display = "block";

                const audio = document.getElementById("bg-audio");
                if (audio) audio.play().catch(() => {});

                launchBalloons();

                confetti({
                    particleCount: 120,
                    spread: 80,
                    origin: { y: 0.6 }
                });

                startAgeTimer();
                spawnHeart();
            } else {
                alert("الباسورد 262026 😉");
            }
        }

        function cutCake() {
            if (!isCakeCut) {
                isCakeCut = true;
                const cake = document.getElementById("cake");
                cake.classList.add("cut");
                
                document.getElementById("cake-status").innerText = "🎉 بالهنا والشفا يا أحلى رنا! كل سنة وأنتِ مع محمد! ❤️";

                confetti({
                    particleCount: 150,
                    spread: 100,
                    origin: { y: 0.5 }
                });
            }
        }

        function launchBalloons() {
            const icons = ['🎈', '🎉', '💖', '✨', '🎈', '❤️'];
            for (let i = 0; i < 25; i++) {
                setTimeout(() => {
                    const b = document.createElement("div");
                    b.className = "balloon";
                    b.innerText = icons[Math.floor(Math.random() * icons.length)];
                    b.style.left = Math.random() * 90 + 5 + "vw";
                    b.style.animationDuration = (Math.random() * 3 + 4) + "s";
                    document.body.appendChild(b);
                    setTimeout(() => b.remove(), 7000);
                }, i * 250);
            }
        }

        function spawnHeart() {
            const board = document.getElementById("game-board");
            const heart = document.getElementById("heart");
            
            const maxX = board.clientWidth - 50;
            const maxY = board.clientHeight - 50;

            heart.style.left = Math.floor(Math.random() * maxX) + "px";
            heart.style.top = Math.floor(Math.random() * maxY) + "px";
            
            let speed = Math.max(600, 1500 - (level * 150));
            clearInterval(gameInterval);
            gameInterval = setInterval(spawnHeart, speed);
        }

        function catchHeart(e) {
            e.stopPropagation();
            score++;
            document.getElementById("score").innerText = score;

            if (score % 5 === 0) {
                level++;
                document.getElementById("level").innerText = level;
                confetti({ particleCount: 40, spread: 50 });
            }

            const msgs = ["Mohamed بيحبك يا رنا! ❤️", "رنا ملكة قلبي! 👑", "Mohamed & Rana Forever! 💘"];
            document.getElementById("game-status").innerText = msgs[Math.floor(Math.random() * msgs.length)];

            spawnHeart();
        }

        function missClick() {
            document.getElementById("game-status").innerText = "ركزي يا رنا القلوب سريعة! 😜";
        }

        // حساب عداد عمر رنا من 18/8/2010
        function startAgeTimer() {
            setInterval(() => {
                const now = new Date();
                
                let years = now.getFullYear() - BIRTH_DATE.getFullYear();
                let m = now.getMonth() - BIRTH_DATE.getMonth();
                if (m < 0 || (m === 0 && now.getDate() < BIRTH_DATE.getDate())) {
                    years--;
                }

                const diff = now - BIRTH_DATE;
                const totalDays = Math.floor(diff / (1000 * 60 * 60 * 24));
                const hours = Math.floor((diff / (1000 * 60 * 60)) % 24);
                const minutes = Math.floor((diff / (1000 * 60)) % 60);

                document.getElementById("age-years").innerText = years;
                document.getElementById("age-days").innerText = totalDays;
                document.getElementById("age-hours").innerText = hours;
                document.getElementById("age-minutes").innerText = minutes;
            }, 1000);
        }
    </script>
</body>
</html>
